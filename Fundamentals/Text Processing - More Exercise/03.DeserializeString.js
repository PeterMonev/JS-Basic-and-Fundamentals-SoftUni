function deserializeString(input) {
     
    let commnad = input.shift();

    while(commnad[0] != 'end'){
       let char = commnad.split('/')

        commnad.shift();
   }
    


}
deserializeString(['a:0/2/4/6',
'b:1/3/5',
'end']
);