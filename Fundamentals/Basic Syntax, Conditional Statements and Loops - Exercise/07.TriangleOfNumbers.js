function triangleOfNumbers(num){
   

    for(let i = 1 ; i <= num ; i++){
       let prinLine = "";
        for(let j = 1 ; j <= i ; j++){
            prinLine += (i + ' ')
            
            
            
        }

 console.log(`${prinLine}`)
    }
   
}
triangleOfNumbers(3);